### Environment Variables
### Generating the `REACT_APP_ENCRYPTED_KEY`

1. Open the `extractCredentials.js` file in the project directory.
2. Update the `secretKey` and `value` variables with the appropriate values.
3. Run the following command in your terminal to generate the encrypted key:
   `node extractCredentials.js`