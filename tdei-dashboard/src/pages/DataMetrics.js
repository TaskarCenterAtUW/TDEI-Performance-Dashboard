import React from 'react';
import { Typography, Box } from '@mui/material';

const DataMetrics = () => {
  return (
    <Box sx={{ minHeight: '100vh' }}>
      <Typography variant="h4">Data Metrics</Typography>
      <p>Details about Data Metrics go here...</p>
    </Box>
  );
};

export default DataMetrics;
